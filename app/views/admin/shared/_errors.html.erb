<% if record.errors.any? %>
  <div class="error-summary" role="alert">
    <h2><%= pluralize(record.errors.count, "erreur", plural: "erreurs") %> à corriger</h2>
    <ul>
      <% record.errors.full_messages.each do |message| %><li><%= message %></li><% end %>
    </ul>
  </div>
<% end %>
